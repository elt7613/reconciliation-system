# Production API base URL (empty in local dev — the Vite dev server proxies
# /api to http://localhost:8000; see vite.config.ts).
# On Dokploy set this to the backend app's public domain, e.g.
# VITE_API_BASE_URL=https://api.yourdomain.com
VITE_API_BASE_URL=
